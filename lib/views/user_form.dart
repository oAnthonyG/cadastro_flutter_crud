import 'package:flutter/material.dart';

class UseForm extends StatelessWidget {
  const UseForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('User Form'),
      ),
      body: Center(
        child: Text('User Form Screen'),
      ),
    );
  }
}